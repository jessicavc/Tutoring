//figure out most amount of songs that can be in a playlist
//playlist will always be 60mins long
//create an array of numbers representing number of songs that fit into playlist

var sixtyMinsPlaylist = [];

//input parameter setting playlist at 60mins.
//return amount of songs that list on 60mins playlist. ex 60 1min songs will return 60

function songLength(songArray) {
  var playlist = 0;
  for (var i = 0; i < songArray.length; i++) {
      //does songArray.length = 60?
      //
  }
}
