//write a function that determines if input is a palindrome (word is same read forwards and backwards)


//racecar

//register

// function palindrome(str) {
//     var str = str.toLowerCase();
//     var j = (str.length - 1);

//     for (var i = 0; i < str.length; i++) {
//         if (str[i] !== str[j]) {
//             return ("this is not a palindrome") 
            
//         } else {
//             j--
//         } 

//     } return ("this is a palindrome");
// }

// palindrome();

//function called reverseWords 
//input "Just Keep Swimming" - return "Swimming Keep Just"


function reverseWords(str) {
    var str = str.toLowerCase();

    for (var i = str.length; i = 0; i--) {
        if (str) {}
           
    }
}
//go through string backwards to find spaces 